import 'dart:ui';

class Dimentions {
  static double cursorSize = 20.0; // Default cursor size
  static double smallCursorSize = 10.0; // Smaller cursor when clicked
  static double pageWidth = 800.0; // Example width (adjust as needed)
  static double pageHeight = 600.0; // Example height (adjust as needed)

  static Offset skillsOffset = const Offset(100, 200); // Example position for SkillsWidget
  static Offset servicesOffset = const Offset(300, 400); // Example position for ServicesWidget
}